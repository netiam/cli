{
  "name": "{{=it.project.name}}",
  "version": "{{=it.project.version}}",
  "description": "{{=it.project.description}}",
  "title": "{{=it.project.name}}",
  "url": "{{=it.project.url}}",
  "sampleUrl": "{{=it.project.url}}",
  "header": {
    "title": "{{=it.project.header.title}}",
    "filename": "{{=it.project.header.filename}}"
  },
  "footer": {
    "title": "{{=it.project.footer.title}}",
    "filename": "{{=it.project.footer.filename}}"
  },
  "order": [
  ]
}
